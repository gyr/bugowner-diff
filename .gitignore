# Python bytecode
__pycache__/
*.pyc

# Virtualenv
.venv/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Coverage output
.coverage
.coverage.*
coverage.xml
htmlcov/

# Build artifacts
dist/
build/
*.egg-info/

# Editor backups and swap files
*~
*.~*~
.#*
*.swp

# Project-specific working data, never tracked (plan section 9):
# the input package list, the plan document itself and the captured
# API probe responses all stay local to the working tree. Anchored to the
# repository root so that fixtures derived from them under tests/ are still
# tracked -- commit 13 builds its integration fixtures from probe-data/.
/16.pkg
/IMPLEMENTATION_PLAN.md
/probe-data/
